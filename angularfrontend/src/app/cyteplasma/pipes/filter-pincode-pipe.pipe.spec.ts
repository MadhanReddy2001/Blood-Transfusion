import { FilterPincodePipePipe } from './filter-pincode-pipe.pipe';

describe('FilterPincodePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPincodePipePipe();
    expect(pipe).toBeTruthy();
  });
});
